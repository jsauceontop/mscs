<li>
	<span class="content"><%= blog.content %></span>
	<span class="timestamp">
		Posted <%= time_ago_in_words(blog.created_at) %> ago.
	</span>
	<% if current_user?(blog.user) %>
		<%= link_to "delete", blog, method: :delete, data: {confirm: "You sure?"}, title: blog.content %>
	<% end %>
</li>